# A00 — Current evidence, test environment and migration discipline

Date / task / slice: 2026-09-23 / A00 / whole task
Agent / branch / source version / claimed files: integration owner (Claude Fable 5.1, T3 Code session) / `t3code/build-sjc-os-plan` / base `b19772a` (origin/main 2026-09-23) / `tests/_harness/*`, `scripts/test-db.mjs`, `db/migrate.mjs`, `db/migrations/0000_*`, `db/migrations/0001_*`, `db/schema.sql` (one FK moved), `lib/commands/*`, `docs/automation-reliability/*`

## Evidence inventory (read-only, 2026-09-23 18:45–19:10 CT)

| Item | Observed | Source |
|---|---|---|
| Live app | `sjcos.service` active, `next start` :3017, BUILD_ID `Ei8xrDD1q0o2VQV8E1qkx`; live checkout `/home/joe/sjcos-app` at `b19772a` with untracked `.hermes-*` scratch files (another session's, left alone) | `systemctl --user`, `git -C /home/joe/sjcos-app` |
| MCP | `sjcos-mcp.service` active (:3018) + 5-min watchdog timer | `systemctl --user list-timers` |
| Timers (9) | agent-retries (10 min), lead-thread-sync (15 min), mcp-watchdog (5 min), push-drain (5 min), lead-first-response (10 min), newsletter-drip (:10), detect (:20), bid-follow-ups (:25), reminders (08:00), comms-watch (09:05) | same |
| Existing live senders | newsletter drip, bid follow-ups (auto-chase, armed per package), lead first response (auto-send OFF by default), push-drain (Telegram to owner only), Approve-click draft send (`lib/approved-draft-send.ts`), owner-grant sends via MCP | `deploy/README.md`, code |
| Existing approvals | `owner_grants` (per action/target, express-permission checkbox, `/engine/permissions`), work-item `approval_status`, portal approvals | `lib/owner-grants.ts` |
| Backups | **None off-host.** `~/sjcos-backups/` holds only Next build rollback dirs and WIP patches; no crontab; no UPS | `ls ~/sjcos-backups`, `crontab -l` |
| Integrations configured | Gmail (OAuth), Telnyx SMS+voice (10DLC), Telegram owner bot, Ollama qwen2.5:7b; **no Square, no QuickBooks, no Stripe** (Settings shows them disconnected) | `.env.local` key names only; `lib/settings.ts` |
| Business snapshot | 4 open leads (13 lost), 10 open projects (7 construction, 1 contract, 1 floor plan, 1 selections) + 41 warranty; 95 subs; A/R outstanding $11,318.26; 99 open work items; profit known on 0 jobs | `business_snapshot` MCP tool |
| Skills / runbooks | 28 skills (26 approved, 2 proposed incl. `fill-project-financials`, `estimate-or-change-order`), 5 runbooks; 5 standing instructions (Townview ignore, vendor/Fiverr 3-bucket rule, scheduled to-dos wait, minimal em dashes, web-lead import) | `list_skills`, `list_runbooks`, `get_standing_instructions` |
| Failure evidence (observed incidents, from memory/docs) | Kleven double-send 2026-09-23 (agent re-sent after Approve had emailed); Kleven no-To-header silent skip 2026-09-22; Larson routing-note leak 2026-09-08; MCP unit crash-loop Aug 3–15 (hardcoded node path); Gmail per-minute quota exhaustion from 150 full thread fetches | memory notes, code comments |
| Audit-scenario findings (not observed) | F01–F16 in TASKS.md remain audit findings, not incidents | plan |
| Test baseline | `npx tsc --noEmit` clean; `npm test` 253 tests, 251 pass, 2 skipped (DB tests needing `FIN_TEST_DATABASE_URL`) | run 2026-09-23 |
| Schema drift | Production has `lead_estimates.alternates jsonb` from unmerged branch `t3code/add-estimate-work-options` (`db/apply-estimate-alternates.mjs`); carried into migration 0001 (idempotent) so fresh = live | `information_schema` diff |
| Fresh-load defect | `db/schema.sql` failed on an empty database (calls.work_item_id FK referenced `work_items` before creation). Fixed by declaring the FK after `work_items`. Fresh load now yields the same 124 tables/columns as production (+`schema_migrations`). | harness |

Unknowns recorded as unknown: actual Telegram delivery reliability today (proven 2026-08-26 per memory, not re-verified here); Hermes gateway availability during this build (not exercised); no production restore has ever been tested.

## Test environment (built)

- `tests/_harness/testdb.mjs`: user-owned Postgres 16 cluster via `initdb`/`pg_ctl`, unix socket only (`listen_addresses=''`), random port, `fsync=off`; loads `db/schema.sql` then the ledger; `assertNotProduction()` refuses any URL shaped like the live DB (`sjcos`@localhost:5432) or lacking the harness `application_name`; `SJC_OUTBOUND_DISABLED=1` set for every test; `SJC_TEST_CLUSTER_TAG` isolates parallel workstreams; `cleanFoundation(client)` resets foundation tables.
- `scripts/test-db.mjs start|reset|url|stop`.
- Existing DB-gated tests (`budget-writes-db`, `estimate-kinds-db`) pass on the harness: `FIN_TEST_DATABASE_URL=$(node scripts/test-db.mjs url | cut -d= -f2-) node --test tests/*-db.test.mjs` → 2 pass.

## Migration discipline (built)

- Historical `db/apply-*.mjs` scripts inventoried (33) and left as the applied baseline; `db/schema.sql` frozen.
- `db/migrate.mjs`: ordered `db/migrations/NNNN_name.sql`, sha256 checksum per file recorded in `schema_migrations`, refuses a changed applied file, advisory lock, one transaction per file, `--status/--dry-run`.
- Reserved number ranges per workstream in `BUILD.md`.

## Exact commands and results

```
node scripts/test-db.mjs reset                      → SJC_TEST_DATABASE_URL=… (schema + 0000 + 0001 applied)
node --test tests/commands-core-db.test.mjs         → 5 pass (V04, V05, V07, V09, V14 foundation checks)
npx tsc --noEmit                                    → clean
npm test                                            → 256 pass, 0 fail, 2 skipped
```

## Setup / activation: not performed. No production migration run (`db/migrate.mjs` has not been executed against the live database). No deploy.

## Rollback: foundation migration 0001 is additive (new tables + two nullable columns); rollback = leave tables in place, deploy prior code.

## Remaining acceptance criteria: file ownership of active teams documented in BUILD.md; the designer team is the in-repo floor designer (shipped 2026-09-21) — no external branch to obtain. Production login for live claims exists via the sjcos MCP (read-only used).

## Next precise ready slice: workstreams launched (see BUILD.md); integration owner wires registrations and A23 after they land.
