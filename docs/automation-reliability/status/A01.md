# A01 — Stable obligations and protected task state

```text
Date / task / slice: 2026-09-23 / A01 / full slice (obligations, protected refresh, review instead of cancel, checkpointed catch-up, collision report)
Agent / branch / source version / claimed files: WS-recovery (Claude) / t3code/build-sjc-os-plan @ 5667e40 (uncommitted, worktree t3code-b015e50c) /
  db/migrations/0002_obligations.sql, lib/obligations/{core,work-items,catchup}.ts, lib/reminders.ts, lib/comms-shared.ts (fileCommsWorkItem only),
  lib/detectors.ts (filing + resolve only), lib/lead-thread-sync.ts (watermark only), scripts/upsert-inbox-work-items.mjs,
  scripts/report-work-item-collisions.mjs, tests/obligations-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Reproduced (by reading, then by test): the inbox sweep matched OPEN items by lower(title) as a third fallback and on every match
  reset status, priority, assignee, due_at, requires_approval; lib/reminders.ts auto-CANCELLED inbox items unseen for 14 days;
  fileCommsWorkItem reset body+priority on every refresh; nothing modelled "one thread, two promises".
  Changed:
   • obligations / obligation_sources tables: business identity keyed by provider+account+thread id+message id; many sources per
     obligation, many obligations per thread; due_at (planned) and deadline_at (contractual) are separate columns.
   • refreshSourcedWorkItem (lib/obligations/work-items.ts) is the ONE filing path for inbox scan, comms and detectors. Refresh of an
     open item touches only body/title (while no agent run/receipt/enrichment exists), last_seen_in_scan_at and EMPTY lead/project links.
     Never status, priority, assignee_kind/key, approval_status, due_at, snoozed_until, promoted_at. Title-based merge removed.
     A new message id on an old thread creates a NEW obligation + item. A message-less replay of a done thread is kept_done (no twin,
     no resurrection). Sources with no stable id → review item (status waiting_on_human, blocked_reason 'unknown source identity; review',
     synthetic stable id so re-scans do not twin it). Comms condition sources (tendlc:*, sms-fail:*) pass `recurring` so a fresh
     occurrence after done still files, exactly as before.
   • lib/reminders.ts: auto-cancel replaced by flagUnseenForReview — 14 days unseen+untouched → waiting_on_human, 'not seen in scan; review',
     stamped once (work_items.scan_review_flagged_at), never cancelled; obligation flips to 'review'. ReminderRun fields renamed
     inboxAgedOut/inboxAgeOutHeld → inboxFlaggedForReview/inboxAwaitingReview (only lib/reminders.ts read them).
   • lib/detectors.ts: dedup/escalation/resolve logic unchanged; filing goes through refreshSourcedWorkItem (source_id = item.sourceId ?? dedupKey,
     recurring) so every detector item carries an obligation; auto-resolve records `detector_cleared` evidence on the obligation.
   • lib/lead-thread-sync.ts: watermark read/advanced through mailbox_checkpoints ('primary'/'lead-thread-sync'); legacy app_settings
     stamp still written and read once as fallback. Paged >150 catch-up lives in lib/obligations/catchup.ts catchUpMailbox (page fetch
     injected; overlap window; checkpoint advanced only after a complete error-free writing run) — not yet wired to a Gmail page fetch
     (fetchThreadMetadata has no pageToken parameter; lib/gmail.ts is not mine).
   • handleReply resolves ONLY the obligation whose origin message the reply answers (In-Reply-To, else the single eligible one);
     other promises on the thread stay open; unmatched inbound → caller files new work; reopen refuses replayed message ids.
  Callers checked: scripts/upsert-inbox-work-items.mjs (rewritten, exports applyBatch/normalizeBatch), lib/comms-shared.ts callers
  (lib/voice.ts, lib/sms.ts, lib/tendlc-watch.ts, lib/call-notes.ts — signature unchanged), lib/detectors.ts runDetectors callers unchanged,
  lib/reminders.ts consumers (none read the renamed fields).
Changed files / ordered migration IDs and checksums:
  db/migrations/0002_obligations.sql  sha256 aebd8f55d75e0f79… (obligations, obligation_sources, work_items.obligation_id,
  work_items.scan_review_flagged_at, mailbox_checkpoints). Additive, IF NOT EXISTS throughout.
Exact verification commands / environment / actual results:
  export SJC_TEST_CLUSTER_TAG=recovery; node scripts/test-db.mjs reset            → schema + 0000…0022 applied (all workstreams' files present)
  node --test tests/obligations-db.test.mjs                                        → 7/7 pass (V01 ×3, V02 ×2, V18, batch+collision report)
  node --test tests/commands-core-db.test.mjs tests/obligations-db.test.mjs tests/runbook-v2-db.test.mjs tests/completion-db.test.mjs
                                                                                    → 19/19 pass, three consecutive runs (no cross-file interference)
  npx tsc --noEmit                                                                  → clean for my files; 3 errors in other workstreams' in-progress
                                                                                    files (app/api/cron/_lib/guard.ts, lib/closeout/checklist.ts, lib/estimating/assembly.ts)
  npx eslint lib/obligations lib/reminders.ts lib/comms-shared.ts lib/detectors.ts lib/lead-thread-sync.ts scripts/upsert-inbox-work-items.mjs
    scripts/report-work-item-collisions.mjs tests/obligations-db.test.mjs           → clean
  npm test                                                                          → 284 pass / 17 fail / 2 skipped; every failure is in
                                                                                    billing/access/measure/commands-core files racing on the shared
                                                                                    sjcos_test database (commands-core passes alone and alongside mine).
Sanitized evidence references / remaining untested conditions:
  tests/obligations-db.test.mjs proves: refresh preserves status/priority/assignee/approval/due/snooze/promoted; agent-touched body frozen;
  same title + different id = two items; done thread not resurrected; new message id = new work; unknown id = review; review flag at most
  once, never cancelled; 412 threads paged at 100/page with checkpoint advance, incremental overlap, error holds watermark; per-obligation
  reply handling; reopen rules; Central snooze on obligation-created items (also via a call follow-up with a future due).
  Untested: live Gmail paging (no pageToken in fetchThreadMetadata), detector run end-to-end (server-only; logic path unchanged), reminders cron.
Setup / activation / deployed version / policy scope (or not performed):
  Not deployed. Activation = apply migration 0002 (node db/migrate.mjs) and restart sjcos.service; inbox cron keeps calling the same script
  (now accepts message_id + mailbox; the scan prompt should supply Gmail message ids to get per-message identity).
Rollback / compatibility / unknown effects requiring reconciliation:
  Rollback = revert the code; tables are additive and can stay. work_items rows filed before this slice have obligation_id NULL — fine.
  Behaviour change to note for Joe: inbox items no longer vanish after 14 days; they surface as waiting_on_human with 'not seen in scan; review'.
  Legacy resolution: run `node scripts/report-work-item-collisions.mjs` on the live DB before/after deploy — it is read-only and prints
  same-title / same-source / unidentified open items for explicit hand resolution; no mass merge exists or is planned.
Remaining acceptance criteria / dependency or setup blocker:
  • Wire catchUpMailbox to a real Gmail page fetch (needs a pageToken-capable list in lib/gmail.ts — not my file; WS owner of gmail.ts or
    integration owner). Until then lead-thread-sync keeps its 150 cap but with the new checkpoint.
  • Inbox scan prompt/skill should emit message_id per item (integration owner / WS-agents).
  • lib/call-notes.ts should call ensureCallFollowUps (see A04.md).
Next precise ready slice:
  Gmail paged fetch (`fetchThreadMetadataPage({ since, pageToken, pageSize })`) + swap lead-thread-sync/detector needs-reply onto catchUpMailbox.
```
