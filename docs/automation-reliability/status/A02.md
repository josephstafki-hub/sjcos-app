# A02 — Transactional runbook start, advance and repair

```text
Date / task / slice: 2026-09-23 / A02 / full slice (pinned definitions, one-transaction start/advance, wakeup outbox, repair, backfill)
Agent / branch / source version / claimed files: WS-recovery (Claude) / t3code/build-sjc-os-plan @ 5667e40 (uncommitted) /
  db/migrations/0003_runbook_v2.sql, lib/completion/runbook-core.ts (pure engine), lib/runbook-engine.ts (pool binding + wakeup drain),
  app/api/internal/runbooks/route.ts, mcp/obligation-tools.mjs (runbook_repair_preview), tests/runbook-v2-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Reproduced: the old engine ran instance insert, work-item insert, instance update and the agent ping as separate autocommit statements
  (a death between them stranded an instance with no step); advance read the LIVE runbook_steps (an edited definition changed a running
  walk); a missing runbook completed the instance as 'done'; no repair path; no notification record.
  Changed:
   • runbook_definition_versions: immutable steps snapshot (checksum in SQL so TS and the 0003 backfill agree); instances pin
     definition_version_id + policy_version at start and read only that.
   • startRunbookTx: advisory xact lock per (runbook, target) → duplicate check → pin version → instance → step-1 (runbook_steps_log
     UNIQUE(instance_id, step_order) claimed first) → work item → instance progress → runbook_wakeups row, all on one `run`.
   • advanceRunbookTx: SELECT … FOR UPDATE on the instance; no pinned version → repair_state needs_review (never done); judges the step
     item exactly as before (done + approved when required; cancelled → instance cancelled; otherwise honest waiting status); validates
     predecessor evidence against the step's required_evidence via agent_receipts (evidence_missing → blocked_reason, no advance);
     inserts the successor under the unique step-log key (loser of a race gets ON CONFLICT DO NOTHING and stands down); records the wakeup.
   • drainRunbookWakeups (lib/runbook-engine.ts): after commit, claims pending wakeups FOR UPDATE SKIP LOCKED, calls pingAgentWorkItem /
     notifyOwner OUTSIDE the transaction, marks sent or leaves pending for the next drain (5 attempts → failed). Route action 'drain'
     exists for a timer as the recovery path. Network calls never happen inside a transaction anymore.
   • repairRunbookInstancesTx: live instances whose current step has no work item; dryRun (default) only logs to runbook_repairs;
     real recreates exactly that step (re-using the existing log row when its work item was deleted), sets repair_state 'repaired';
     unpinned/missing definition → needs_review. Bounded by `limit`, replay-safe.
   • Backfill in 0003: version 1 for every existing runbook; active instances pinned to it with policy_version 'legacy-pinned-at-0003'
     (recorded as legacy, not proven); instances whose runbook is gone → needs_review; runbook_steps_log backfilled from existing step
     work items (earliest per step) so repair never recreates a step that exists.
  Public exports kept with the same signatures: startRunbook, advanceRunbookInstance (now returns the AdvanceOutcome), maybeAdvanceRunbook,
  cancelRunbookInstance, getActiveRunbookInstances, getActiveRunbookInstancesFor, RunbookInstanceView (added repairState, blockedReason,
  definitionVersion; step count/title now read from the pinned version). Callers checked: lib/intake.ts, lib/actions/projects.ts,
  lib/approve-work-item.ts, lib/actions/today.ts, lib/actions/record-ops.ts, lib/orchestrator/execute.ts, lib/actions/engine.ts,
  app/(os)/engine/page.tsx, project/lead pages, components/engine/EngineClient.tsx (type-only), mcp/runbook-tools.mjs (route unchanged
  for start/advance).
Changed files / ordered migration IDs and checksums:
  db/migrations/0003_runbook_v2.sql  sha256 b50441b7556f63b0… (runbook_steps.required_evidence, runbook_definition_versions,
  runbook_instances.{definition_version_id,policy_version,blocked_reason,repair_state}, runbook_steps_log, runbook_wakeups,
  runbook_repairs, backfill). Additive; constraints added inside DO blocks so re-runs are safe.
Exact verification commands / environment / actual results:
  export SJC_TEST_CLUSTER_TAG=recovery; node scripts/test-db.mjs reset → ok (5 seeded runbooks get version 1; test asserts none missing)
  node --test tests/runbook-v2-db.test.mjs → 4/4 pass:
    V03 start: simulated death after each of the N statements of a start leaves 0 rows in instances/work_items/steps_log/wakeups;
      5 concurrent starts → exactly 1 instance, 1 item, 1 log row, 1 wakeup, version pinned (policy_version def@1).
    V03 advance: death after each statement leaves counts and current_step untouched; 5 concurrent advances → 1 successor;
      runbook edited mid-run (step 2 renamed, step 4 added) → step-2 item carries the pinned title and "step 2 of 3"; the instance
      finishes after step 3; step marked done without required 'draft' receipt → evidence_missing (blocked_reason set), with a receipt
      → advanced to the human step with an owner_notify wakeup; wakeups claim/finish.
    V03 repair: dry-run lists recreate_step and creates nothing (logged); real run recreates the one step, no duplicate log row,
      repair_state repaired; rerun scans 0; legacy unpinned instance → advance and repair both needs_review, status stays running;
      cancel closes items and voids pending wakeups.
    Backfill: every seeded runbook has a version row with a 64-hex checksum.
  npx tsc --noEmit → clean for my files (3 unrelated in-progress errors from other workstreams, listed in A01.md)
  npx eslint lib/completion lib/runbook-engine.ts app/api/internal/runbooks/route.ts mcp/obligation-tools.mjs tests/runbook-v2-db.test.mjs → clean
Sanitized evidence references / remaining untested conditions:
  The pure core is fully harness-tested. Untested live: drainRunbookWakeups against real pingAgentWorkItem/notifyOwner (server-only;
  same calls the old engine made, now outside the transaction), the /engine UI rendering of repairState/blockedReason (fields added,
  UI not changed), and route actions complete/repair/drain over HTTP (no app process in the harness).
Setup / activation / deployed version / policy scope (or not performed):
  Not deployed. Activation: node db/migrate.mjs (0002+0003) → restart sjcos.service. Optional: a timer posting
  {action:"drain"} to /api/internal/runbooks every few minutes as the wakeup recovery path (deploy/ files are not mine —
  integration owner). Leases for the drain come from SKIP LOCKED + attempts, not the shared worker contract (A03b) yet.
Rollback / compatibility / unknown effects requiring reconciliation:
  Code rollback restores the old engine, which ignores the new columns; existing instances keep working (columns are additive).
  Instances marked needs_review by the backfill (runbook missing) require an owner decision; nothing is auto-completed.
Remaining acceptance criteria / dependency or setup blocker:
  • Integration owner: register `mcp/obligation-tools.mjs` in mcp/sjcos-mcp.mjs buildServer():
      import { registerObligationTools } from "./obligation-tools.mjs";  registerObligationTools(server, { rows, json, appCall: runbooksCall });
  • Integration owner / WS-worker: a timer for the 'drain' action (or fold into the supervised worker loop).
  • A real "repair" button (dry-run preview exists as an MCP tool; the real run is route action repair {dry_run:false} — owner-only UI not built).
Next precise ready slice:
  Move the drain onto the A03b worker lease contract; add the /engine repair preview/run block.
```
