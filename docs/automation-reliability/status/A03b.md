# A03b — Durable intake and supervised worker

```text
Date / task / slice: 2026-09-23 / A03b / full slice (durable intake, supervised worker loop, registry + health, cron-run ledger, fake provider)
Agent / branch / source version / claimed files: WS-worker (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/worker/{intake,loop,registry,health,cron-runs,telnyx-processor,fake-provider,types}.ts, lib/worker/{load-app,node-hooks}.mjs,
  lib/commands/source-events.ts (shared foundation), scripts/sjcos-worker.mjs, deploy/sjcos-worker.{service,sh},
  app/api/cron/_lib (recordCronRun guard), db/migrations/0020_cron_runs.sql, tests/worker-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: webhooks (Telnyx/Gmail) were processed inline in the request; a persistence outage lost the event, a duplicate or
    out-of-order delivery re-ran side effects, a hung handler had no lease/heartbeat, cron ticks left no record when they died.
  After: verified signature → persistInbound() writes ONE source_events row (unique on provider+event id) BEFORE any 200;
    a persistence failure answers 503 (provider retries); duplicates answer 200 without re-processing; the worker
    (scripts/sjcos-worker.mjs, systemd sjcos-worker.service, Restart=always) claims events with a lease + heartbeat,
    processes through registered processors, records attempts, retries with backoff and parks poison events; registry +
    heartbeat rows (workers) feed workerHealth(); recordCronRun()/cronJobHealth() give every cron route a durable tick record.
  Callers checked: app/api/telnyx/* and app/api/inbox webhooks → persistInbound; lib/dispatch (claims intents from the same loop);
    app/api/cron/* routes wrapped by _lib guard (records cron_runs).
Changed files / ordered migration IDs and checksums:
  0001 (source_events, workers) 4c78318f2a17 · 0020_cron_runs.sql 63acd2913e38
Exact verification commands / environment / actual results:
  export SJC_TEST_CLUSTER_TAG=worker; node --test tests/worker-db.test.mjs → 9/9 pass
    V04 bad/replayed signature refused before persistence; outage → no acceptance; duplicate → 200 no re-run; out-of-order
      events keep order by provider sequence; lost wakeup → recovered by the loop's poll.
    V07 worker part: stale lease re-claimed once; V12 hung run: lease expiry + heartbeat stop → re-queued, attempt counted.
  npm test (full, 2026-09-23 23:5x) → 404 tests, 402 pass, 0 fail, 2 skipped.
Operational state: implemented on branch. NOT deployed (unit not installed), NOT enabled, NOT proven.
Open: install deploy/sjcos-worker.service on the host (deploy/README.md packet); Gmail push (watch) still polls via lead-thread-sync.
```
