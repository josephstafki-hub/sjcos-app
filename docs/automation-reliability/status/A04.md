# A04 — Evidence-backed completion

```text
Date / task / slice: 2026-09-23 / A04 / full slice (completeWorkItem command, evidence contracts, call follow-ups, route + MCP tools)
Agent / branch / source version / claimed files: WS-recovery (Claude) / t3code/build-sjc-os-plan @ 5667e40 (uncommitted) /
  lib/completion/complete.ts, lib/completion/call-followups.ts, app/api/internal/runbooks/route.ts (action "complete"),
  mcp/obligation-tools.mjs, runbook_steps.required_evidence (in 0003), tests/completion-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Reproduced: update_work_item_status (MCP), lib/orchestrator/execute.ts mark_done and the owner UI all flip status='done' with no
  evidence; receipts are optional and free-text; call-notes finish() refiled every action item on retry.
  Changed:
   • completeWorkItem(run, { workItemId, principal, evidence, agentRunId?, note? }): FOR UPDATE on the item; reads the step's contract
     from the PINNED definition (never the live table); validates typed evidence:
       draft → knowledge_items / document_drafts row must exist (a label is not an artifact);
       provider_accepted → action_intents.state ∈ {accepted, confirmed}; delivered → state = confirmed (accepted ≠ delivered);
       business_response → a RESOLVED obligation or an existing source_events row;
       manual → actor + reason (receipt has uri NULL; nothing invented);
       record → whitelisted table + id + revision equal (ms precision) to the row's current updated_at/revision, else "stale".
     Contract mismatch, missing/stale/wrong evidence → { ok:false, error } and nothing written. Valid → ONE agent_receipts row
     (receipt_kind = evidence kind, metadata = evidence + redacted principal + contract), status done, linked obligation resolved with
     work_item_done evidence, runbook advanced on the same transaction (advanceRunbookTx). Already-done → ok, alreadyDone:true, no receipt.
     Legacy items (required 'any') accept manual with actor+reason and report legacy:true.
   • ensureCallFollowUps(run, callId, { by }): per (call, action kind) keys `call:<id>:action:<i>` (work items via refreshSourcedWorkItem,
     obligation kind call_action) and `call:<id>:voicemail-note` marker in the callback item's body; advisory xact lock per call; a re-run
     creates only the missing actions, never twins, never resurrects a done action.
   • Route action "complete" (bearer CRON_SECRET → servicePrincipal; optional agent + on_behalf_of_user_id → agentPrincipal looked up
     server-side); drains runbook wakeups after commit. MCP tool complete_work_item_with_evidence proxies to it.
  Callers checked: mcp/sjcos-mcp.mjs update_work_item_status / submit_draft_for_approval (read only — still write status directly and
  call "advance"; see below), lib/orchestrator/execute.ts (not mine, unchanged), lib/call-notes.ts (not mine, unchanged).
Changed files / ordered migration IDs and checksums:
  0003_runbook_v2.sql (shared with A02): runbook_steps.required_evidence text NOT NULL DEFAULT 'any' CHECK (...), carried into every
  pinned definition snapshot. No separate migration.
Exact verification commands / environment / actual results:
  export SJC_TEST_CLUSTER_TAG=recovery; node --test tests/completion-db.test.mjs → 3/3 pass:
    V06: null evidence, wrong kind for a 'draft' step, nonexistent knowledge item, label-only draft → refused, status queued, 0 receipts;
      real knowledge item → done + 1 receipt + successor spawned in the same tx; identical retry → alreadyDone, still 1 receipt, still
      1 successor; step needing provider_accepted refuses a pending intent, refuses 'delivered' on an accepted intent, accepts
      provider_accepted and completes the instance; receipt uri = provider_ref.
    V06 record/manual: stale revision refused, non-whitelisted table refused, manual without reason refused; legacy manual accepted
      with legacy:true, receipt uri NULL, actor/reason in metadata; linked obligation resolved with the receipt id;
      business_response refused until the obligation is resolved, then accepted.
    V06 call follow-ups: 3 action items (2 Joe's) → 2 items + voicemail note; delete one → rerun creates only that one, note already
      present; third run creates nothing; a done action is not recreated; a future-dated action is snoozed by the Central trigger.
  npx eslint lib/completion tests/completion-db.test.mjs → clean; npx tsc --noEmit → clean for my files.
Sanitized evidence references / remaining untested conditions:
  Route + MCP proxy not exercised over HTTP (no app process in the harness); document_drafts branch validated by SQL shape only
  (test uses knowledge_items).
Setup / activation / deployed version / policy scope (or not performed):
  Not deployed. Needs 0003 applied and sjcos + sjcos-mcp restarted (after the owner registers the tool module).
Rollback / compatibility / unknown effects requiring reconciliation:
  Pure addition; old completion paths still work (they bypass evidence — that is the remaining gap, not a breakage). required_evidence
  defaults to 'any' so no existing step is blocked; Joe sets stricter contracts per step when ready.
Remaining acceptance criteria / dependency or setup blocker (for the integration owner / file owners):
  • mcp/sjcos-mcp.mjs update_work_item_status: when status === 'done', call runbooksCall("complete", { work_item_id, evidence, agent:"mcp" })
    instead of the raw UPDATE; accept an optional `evidence` input; treat a note-only call as { kind:"manual", actor:<agent>, reason:note }
    so legacy use keeps working and is reported. submit_draft_for_approval can pass { kind:"draft", knowledge_item_id } once the draft
    knowledge item is inserted.
  • lib/orchestrator/execute.ts mark_done / update_status → withTransaction(run => completeWorkItem(run, …)) with a server-derived principal.
  • lib/call-notes.ts finish(): replace the fileCommsWorkItem loop + voicemail append with
      await withTransaction((run) => ensureCallFollowUps(run, call.id, { by }));
    (import { withTransaction } from "@/lib/commands/db"; import { ensureCallFollowUps } from "@/lib/completion/call-followups").
  • "Ambiguous person/date/project becomes a question" — not built here; belongs with the ask_owner path.
Next precise ready slice:
  Owner UI "Mark done" → completeWorkItem with manual evidence (actor = session user, reason from the form), then flip the three
  callers above.
```
