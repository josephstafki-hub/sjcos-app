# A05/A06 — Approval binding and duplicate-safe external actions

```text
Date / task / slice: 2026-09-23 / A05+A06 / full slice (decisions binding, action intents/attempts, dispatcher, provider adapters, grant gate)
Agent / branch / source version / claimed files: WS-approvals (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/commands/{intents,decisions}.ts (foundation), lib/decisions/{resolve,cards,grants,notify,routine,telegram,actions}.ts,
  lib/dispatch/{core,db,effects,kinds,authority}.ts, lib/providers/{email,sms,telegram,voice,types,index}.ts,
  lib/authority/mcp-gate.ts + principalMaySpendGrant, app/api/internal/owner-grants (perform gate), app/api/telegram callback,
  app/(os)/engine/decisions, mcp/decision-tools.mjs, deploy/sjcos-dispatch.*, db/migrations/0004_decision_surfaces.sql,
  tests/dispatch-db.test.mjs, tests/providers.test.mjs, tests/grant-gate.test.mjs, tests/approved-draft-rules.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: an approval was a boolean on a work item; content/recipient/amount could change after the tap; a timeout after the
    provider accepted was retried blind (duplicate email/SMS); the same tap from Telegram and the app could fire twice; a
    revoked grant kept working; bulk sends re-sent to recipients that had already succeeded or opted out.
  After: decisions bind exact content hash + recipient + amount + artifact revision (stageDecision/resolveDecision, first tap
    wins across app/Telegram/push/MCP via a single UPDATE … WHERE status='pending'); consuming an approved decision enqueues
    action_intents keyed (kind, target, content hash); dispatcher leases → attempt → accepted → confirmed; unknown outcome
    (timeout after write) is HELD (state unknown) and reconciled before any retry; permanent 4xx fails; 429 retries; per-recipient
    intents for bulk sends (a success is never repeated, an opt-out recorded before dispatch refuses that recipient);
    grants are person-bound (principalMaySpendGrant) and checked at perform time — revocation/expiry/excess amount refuse.
  Callers checked: send_* MCP tools (grant gate + work_item dup guard), app Approve (lib/actions/*), Telegram callback,
    push drain, lib/decisions/notify announceUnannouncedDecisions (dispatcher pass announces cards staged outside the app).
Changed files / ordered migration IDs and checksums: 0001 4c78318f2a17 · 0004_decision_surfaces.sql b7f52c1bf352
Exact verification commands / environment / actual results:
  node --test tests/dispatch-db.test.mjs → 4/4 (V07 accepted-then-timeout held; stale worker; duplicate callback = one effect;
    V08 partial bulk success + opt-out); tests/providers.test.mjs → 7/7 (classifyTransportError, adapters idempotent on ref);
    tests/grant-gate.test.mjs → 7/7 (V09 changed content/payee/amount, expiry, revocation, concurrent taps, unknown outcome does
    not restore spent authority); tests/approved-draft-rules.test.mjs → 16/16.
Operational state: implemented on branch. Dispatcher timer NOT installed on the host; live sends still go through the existing
  approve paths (which now stage decisions where wired). NOT proven.
Open: install deploy/sjcos-dispatch.timer; Telegram inline buttons need the bot callback route deployed (A10).
```
