# A07a — Small independent invoice integrity repair

```text
Date / task / slice: 2026-09-23 / A07a / full slice (economic key, collision report, no renumbering)
Agent / branch / source version / claimed files: WS-money (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  db/migrations/0006_invoice_identity.sql, lib/billing/core.ts (invoiceCollisionReport, economic key helpers),
  scripts/invoice-collision-report.mjs, mcp/billing-tools.mjs (invoice_collision_report), tests/billing-db.test.mjs (identity cases)
Failure reproduced / changed behavior / callers checked:
  Before: invoices had no economic identity; two draws with the same label or amount on one project could be created by a retry;
    numbers were display-only.
  After: invoices.economic_key (project + milestone key + draw revision) with a partial unique index; issuance is keyed
    (one obligation per key); existing rows keep their numbers — the collision report lists label/amount collisions and rows
    without a key for EXPLICIT owner resolution (nothing is merged or renumbered by code).
Changed files / ordered migration IDs and checksums: 0006_invoice_identity.sql a3b8526971ea
Exact verification commands / environment / actual results:
  node --test tests/billing-db.test.mjs → 6/6 (V10 concurrent milestone → one invoice; contract revision → new key, old
    invoice untouched); node scripts/invoice-collision-report.mjs against the harness → empty report.
Operational state: implemented on branch; migration NOT applied to production; report NOT yet run against production data.
Open: run the collision report on the live DB after deploy and resolve legacy collisions with Joe.
```
