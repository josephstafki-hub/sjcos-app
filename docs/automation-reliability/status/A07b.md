# A07b — Invoice lifecycle, balances and automatic contract billing

```text
Date / task / slice: 2026-09-23 / A07b / full slice (lifecycle, verified balances, deliveries, automatic issuance under policy, reminders)
Agent / branch / source version / claimed files: WS-money (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/billing/{core,commands,reminders,terms,server}.ts, db/migrations/0006 + 0007_payments.sql (invoice_payments, invoice_deliveries),
  mcp/billing-tools.mjs, app/api/cron/reminders (balance-aware), lib/actions/esign.ts (billingOnSignatureSigned),
  tests/billing-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: status ∈ draft/sent/paid; a failed email marked sent; partial payments, credits and returns had nowhere to live;
    reminders fired on invoices already paid by check; the retainer/draw/final invoices were typed by hand.
  After: invoice_payments ledger (payment/credit/return/refund, settled vs pending) → verifiedBalances(); invoice_deliveries
    (a failed send never marks sent); reminders stop at zero balance and hold on disputes/pending payments; automatic issuance
    under active policies: client acceptance of the exact offer → agreement + initial invoice once; owner milestone confirmation
    → progress invoice once; written sign-off → final invoice for the verified remainder (approved CO balances, payments,
    credits applied once). No policy → staged as a decision with the missing policy named.
  Callers checked: esign applySignature → billingOnSignatureSigned; field hooks issueProgressOnOwnerConfirmation; closeout
    sign-off; MCP record_manual_payment (person required).
Changed files / ordered migration IDs and checksums: 0006 a3b8526971ea · 0007_payments.sql 978efdf401fd
Exact verification commands / environment / actual results:
  node --test tests/billing-db.test.mjs → 6/6: V10 failed send never marks sent; partial pay / credit / return sequences;
    zero balance stops reminders; V38 duplicate acceptance = one retainer; V44 duplicate sign-off = one final invoice.
Operational state: implemented on branch. Issuance policies are NOT active (no policies rows on production); NOT deployed.
Open: Joe activates billing.auto_issue policies on /engine (scope: new jobs) after deploy; legacy invoices keep manual flow.
```
