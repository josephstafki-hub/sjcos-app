# A08a — Immediate business-agent access restrictions

```text
Date / task / slice:
  2026-09-23 / A08a / full slice (run profiles, MCP identity, credentials inventory, staff = business only)
Agent / branch / source version / claimed files:
  WS-access (Claude) / t3code/build-sjc-os-plan (worktree t3code-b015e50c) / base edac657
  scripts/run-claude-agent.mjs, lib/dev-agents.ts, lib/dev-agents-meta.ts (read only — no change needed),
  lib/authority/run-profile.mjs, lib/authority/mcp-gate.ts, mcp/sjcos-mcp.mjs (auth section only),
  lib/permissions.ts, docs/users-and-access.md, docs/automation-reliability/credentials-inventory.md,
  tests/agent-profile.test.mjs, db/migrations/0009_access.sql (shared with A22)
Failure reproduced / changed behavior / callers checked:
  Before: every Ask-window Claude run — including one started by a STAFF account holding `ai` — ran as Joe's
    full operator: cwd = repo, --add-dir repo, Bash/Edit/Write, WebFetch, the app's whole process env (all
    secrets), no time or turn limit; the MCP server had no idea who was behind a call.
  After (enforced by flags/spawn options, not prompt text):
    • Two profiles (lib/authority/run-profile.mjs): profileFor(starter, requested) — owner → 'operator'
      (unchanged behaviour) unless a business run is requested; staff / nobody → 'business'. Staff can never
      get 'operator' (startClaudeRun throws; the runner independently downgrades a row whose person is not an
      active owner).
    • startClaudeRun (lib/dev-agents.ts) reads the starter from the SESSION (getCurrentUser; outside a request
      → unattended), stores dev_agent_runs.profile + principal_user_id, refuses past the hourly cap for business
      runs (warns owner runs), and mints the run grant ONLY for an owner's Express-permission tick.
    • Runner (scripts/run-claude-agent.mjs), business profile: `--restricted`, `--disallowedTools Bash Write
      Edit MultiEdit NotebookEdit WebFetch WebSearch Task Agent EnterWorktree ExitWorktree`, `--add-dir
      <repo>/docs/automation-reliability` only (no repo), cwd = os.tmpdir()/sjcos-agent-<run> (removed after),
      permission mode never bypass/auto/dontAsk, minimal env (PATH/HOME/USER/LANG/TERM/TMPDIR/XDG/NODE_OPTIONS +
      SJC_* tags — none of DATABASE_URL/CRON_SECRET/SESSION_SECRET/GMAIL_*/TELNYX_*/TELEGRAM_*), sjcos MCP by
      absolute path, no Playwright server, 20-minute sliding deadline (DEV_CLAUDE_TIMEOUT_MS still overrides),
      60 assistant-turn cap (runner-counted: CLI 2.1.278 has no --max-turns), `--max-budget-usd` from
      app_settings agent.max_cost_per_run_usd, business prompt states the profile + ACTING FOR line, and
      assertBusinessArgs() refuses to spawn if the restrictions are missing or a repo --add-dir slipped in.
      Operator profile: identical to before.
    • MCP identity (mcp/sjcos-mcp.mjs auth section): currentPrincipal() — stdio reads SJC_PRINCIPAL_USER_ID /
      SJC_RUN_STARTED_S / SJC_RUN_PROFILE / SJC_RUN_ID from env; HTTP reads X-SJC-Principal-User (+ X-SJC-Run-
      Started-S / -Run-Id / -Run-Profile) ONLY after the MCP_HTTP_TOKEN bearer check, bound per request with
      AsyncLocalStorage. The ROLE is always looked up from the users row per call (never from env/header);
      revoked/disabled → refused. principal_user_id / principal_role / principal_revoked / run_id / run_profile
      are threaded into every internal-route payload (grantsCall, runbooksCall, newsletterCall, biddingCall,
      docDraftsCall, leadsCall, poCall via one internalCall helper). grantsCall("perform") refuses a staff
      principal without matching authority (principalMaySpendGrant over the MCP pool; lazy .ts import; if the
      import fails → staff refused, fail closed). No person behind the call → unchanged (the owner grant alone
      decides, as before, so claude.ai / Hermes / plain stdio keep working).
    • lib/permissions.ts `ai` description: business profile only; docs/users-and-access.md updated.
  Callers checked: lib/actions/ai-chat.ts sendMessageAction (requireAccess('ai') → startClaudeRun: now
    profile-aware with no signature change); pingAgentWorkItem / retryFailedApprovalPings / orchestrator callers
    (extras unchanged; unattended → business); mcp/grants-tools.mjs (calls grantsCall("perform") — gate applies);
    mcp/interact-mcp.mjs (env tags unchanged, new ones ignored).
Changed files / ordered migration IDs and checksums:
  0009_access.sql (see A22.md) — dev_agent_runs.profile / principal_user_id, agent_usage.
Exact verification commands / environment / actual results:
  node --test tests/agent-profile.test.mjs → 8/8: staff/unattended → business; owner → operator; business args
    contain --restricted + the disallowed tools + docs-only --add-dir + scratch cwd + caps and NO repo --add-dir;
    assertBusinessArgs throws on a repo --add-dir / missing flags; bypassPermissions coerced; admission cap;
    worker registry; catalog. `node --check` + eslint on the runner and MCP server: clean.
  `claude --help` (2.1.278) confirmed: --restricted, --disallowedTools, --add-dir, --max-budget-usd exist;
    --max-turns does not (hence the runner-side cap).
  NOT run live: an actual `claude -p --restricted` business run against the dev app (no services started per the
    rules). Smoke for the integration owner after deploy: start a run as a staff test account and ask it to
    "run `id`" and "read .env.local" — both must come back refused/denied; the activity log must show the
    business profile note; dev_agent_runs.profile = 'business' and principal_user_id = the staff id.
Sanitized evidence references / remaining untested conditions:
  docs/automation-reliability/credentials-inventory.md — every secret by NAME, who needs it, what a business
  run can no longer reach, and the honest residuals (MCP server still on the full DATABASE_URL until
  deploy/db-roles.sql is applied; --restricted is a CLI feature; HOME is passed; operator runs unchanged;
  prompt lines informational; Hermes/Qwen paths untouched).
Setup / activation / deployed version / policy scope (or not performed):
  Not deployed. After merge: migrate (0009), build + restart sjcos.service (runner + dev-agents), restart
  sjcos-mcp.service (auth section; stdio spawns pick the new file up automatically). Optional app_settings:
  agent.max_cost_per_run_usd, agent.max_runs_per_hour (blank = off).
Rollback / compatibility / unknown effects requiring reconciliation:
  Runner change is self-contained; an old runner ignores the new columns (defaults). If the CLI's --restricted
  ever rejects a combination, business runs fail visibly (Claude CLI failed: …) rather than opening up. MCP
  identity fields are additive in payloads — internal routes that don't read them are unaffected.
Remaining acceptance criteria / dependency or setup blocker:
  • WS-approvals: app/api/internal/owner-grants/route.ts "perform" — add before performGrantedAction():
      const gate = await principalMaySpendGrant(runDirect, str("principal_user_id") ?? null, gatedAction,
        { projectId: str("project_id") ?? null, amountCents: body.amount_cents == null ? null : Number(body.amount_cents) });
      if (!gate.ok) return NextResponse.json({ ok: false, error: gate.reason }, { status: 403 });
    (import { principalMaySpendGrant } from "@/lib/authority/mcp-gate"; import { runDirect } from "@/lib/commands/db").
    Same one-liner for the other internal routes that act on the person's behalf: derive the principal with
    agentPrincipal(agent, { onBehalfOfUserId: body.principal_user_id }) instead of a service principal.
  • Hermes (hermesChat / Telegram) and Qwen are not on this runner — their business scope is A24 (WS-agents).
  • A dedicated Unix user for business runs (closes the HOME residual) — system change for Joe.
  • lib/actions/dev-agents.ts poll/stop for staff (see A22.md).
Next precise ready slice:
  A08b items already delivered here: see A08b.md. Next: DATABASE_URL_AGENT wiring in mcp/sjcos-mcp.mjs
  databaseUrl() once Joe applies deploy/db-roles.sql.
```
