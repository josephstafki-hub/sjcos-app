# A08b — Enforced worker identities, budgets and recovery

```text
Date / task / slice:
  2026-09-23 / A08b / as far as the current runtime permits (identities, budgets, mid-session revocation, tests)
Agent / branch / source version / claimed files:
  WS-access (Claude) / t3code/build-sjc-os-plan (worktree t3code-b015e50c) / base edac657
  lib/authority/worker-identity.ts, lib/authority/usage.ts, lib/authority/run-profile.mjs (thresholds),
  deploy/db-roles.sql, scripts/run-claude-agent.mjs (usage row, revocation kill, turn cap),
  lib/dev-agents.ts (admitRun, runPrincipalStillValid), db/migrations/0009_access.sql (agent_usage),
  tests/access-db.test.mjs (budgets + gate), tests/agent-profile.test.mjs (workers, admission)
Failure reproduced / changed behavior / callers checked:
  Before: no worker identity concept (everything is "the app"); no metering; a disabled staff account's
    running agent turn continued until its natural end; no run caps.
  After:
    • lib/authority/worker-identity.ts — registry: dispatch, weekly-summary, business-agent-worker, qbo-sync,
      payments-reconcile, backup, monitor; each with allowed intent kinds, lanes and a Postgres role hint;
      assertWorkerMay(worker, kind) throws WorkerScopeError (unknown worker refused); workerPrincipal() is a
      ServicePrincipal, never a user.
    • deploy/db-roles.sql — CREATE ROLE sjcos_agent / sjcos_worker / sjcos_readonly / sjcos_backup with GRANTs
      limited to the tables each needs; users / authority_grants / session_revocations / permission_audit /
      owner_grants / app_settings / policies revoked from the agent role; SECURITY DEFINER functions
      sjcos_principal(uuid) and sjcos_authority(uuid,text,uuid,bigint) so the agent role can identify a person
      and check authority without reading the tables. Joe applies it by hand; nothing runs it.
    • Metering: agent_usage (run id, runtime, model, profile, principal, tokens in/out, cost, duration, turns,
      outcome done/error/stopped/timeout/turn_cap/budget_cap/revoked). The runner writes one row per run from
      the result envelope on every exit path; usage.ts recordUsage() for other runtimes.
    • Thresholds: app_settings agent.max_cost_per_run_usd (business runs → --max-budget-usd; owner runs get an
      activity note) and agent.max_runs_per_hour (admitRun: business refused, owner warned; counted over ALL
      dev_agent_runs in the trailing hour).
    • Mid-session revocation: startClaudeRun re-derives the starter per turn; pingAgentWorkItem refuses a staff
      starter whose sessions were revoked; runPrincipalStillValid(runId) exported for other callers; the runner's
      5-second heartbeat re-reads users.active + session_revocations for the run's person and SIGKILLs the CLI
      (outcome 'revoked', message in the thread); the MCP server's currentPrincipal() re-reads per tool call and
      grantsCall("perform") refuses a revoked id.
    • Finite limits: business runs 20-minute sliding deadline + 60-turn cap + optional dollar cap (A08a).
  Callers checked: retryFailedApprovalPings (unattended → business, admission applies), orchestrator startClaudeRun
    callers (extras unchanged).
Changed files / ordered migration IDs and checksums:
  0009_access.sql (A22.md) — agent_usage + dev_agent_runs.profile/principal_user_id.
Exact verification commands / environment / actual results:
  node --test tests/access-db.test.mjs → "A08b budgets": unset threshold open; cap 2 with 2 runs → business
    refused / operator warned; agent_usage row round-trips; dev_agent_runs.profile CHECK refuses 'root'.
    "A08a MCP gate": owner ok, unattended/garbage id refused, staff needs the mapped kind within project, send_email
    never delegable, revokeSessions after run start → refused, disabled account → refused.
  node --test tests/agent-profile.test.mjs → worker registry least-privilege assertions.
  deploy/db-roles.sql: NOT applied anywhere (by design). Syntax reviewed by eye only; table names match
    db/schema.sql + 0001–0009 where they exist today. Tables that other workstreams add later (invoice_lines,
    payments, qbo_*) may not exist yet — the file says to re-run after those migrations or trim the GRANT list.
Sanitized evidence references / remaining untested conditions:
  Untested: a live runner kill on revocation (needs a running app + CLI); --max-budget-usd stopping a real run
  (needs a paid run). Both are 10-line code paths covered by reading; smoke after deploy.
Setup / activation / deployed version / policy scope (or not performed):
  Not deployed. Enable thresholds by inserting app_settings rows (no UI yet — Settings card is a follow-up).
  DB roles: Joe runs `psql -d sjcos -f deploy/db-roles.sql`, sets passwords, adds DATABASE_URL_AGENT /
  DATABASE_URL_WORKER / DATABASE_URL_READONLY / DATABASE_URL_BACKUP to .env.local; then mcp/sjcos-mcp.mjs
  databaseUrl() and scripts/run-business-agent.mjs should prefer DATABASE_URL_AGENT (one-line each, MCP owner).
Rollback / compatibility / unknown effects requiring reconciliation:
  agent_usage is append-only telemetry; dropping it loses nothing operational. Roles can be DROPped after
  reassigning ownership (`REASSIGN OWNED BY … ; DROP OWNED BY …`).
Remaining acceptance criteria / dependency or setup blocker:
  • "Hung work ends visibly and can recover without duplicate actions": the runner already ends visibly (timeout /
    turn cap / revoked / stopped, each persisted on the row and in the thread) and every external effect is an
    intent with an operation key (WS-approvals dispatcher), so a re-run cannot double-send. The supervised worker
    loop itself (heartbeats, lease recovery) is WS-worker's scripts/sjcos-worker.mjs — it should call
    assertWorkerMay() before claiming intents and connect as its dbRole once roles exist.
  • Enforced DB-level isolation needs deploy/db-roles.sql applied (Joe) — until then isolation is tool-code level.
  • A per-run Settings card for the two thresholds + an agent_usage summary (cost today / this month) — small
    follow-up; keys documented in credentials-inventory.md and 0009_access.sql.
  • Mobile/native: bearer tokens are covered by lib/api-auth.ts; a native push "approve" button that resolves a
    decision must go through resolveDecision (authorityFor) — nothing native exists yet.
Next precise ready slice:
  Settings › AI › "Agent limits" card (two thresholds) + usage summary; DATABASE_URL_AGENT wiring after roles.
```
