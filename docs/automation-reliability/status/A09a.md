# A09a — Off-host backups, basic alerts and restore proof

```text
Date / task / slice: 2026-09-23 / A09a / full slice (encrypted off-host backups, retention, failed-run alerts, scripted restore)
Agent / branch / source version / claimed files: WS-worker (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/backup/{crypto,retention,runs,status,targets}.ts, scripts/backup.mjs, scripts/restore.mjs, deploy/sjcos-backup.*,
  deploy/sjcos-backup-db.*, db/migrations/0019_backup_runs.sql, docs/recovery.md, tests/backup-restore.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: no off-host copy; a stale or failed backup was silent; restore had never been exercised.
  After: db/files/config sets encrypted (age or openssl) to dir:/rclone:/ssh: targets; missing target or passphrase → non-zero
    exit AND a failed backup_runs row (V13); retention 14d/8w/6m (monthly keeps the oldest set); backupHealth() stale/failed
    → owner alert through the monitor; restore.mjs restores a set into a fresh DB + dir and runs integrity checks, marking
    restore_tested_at only after a real restore.
Changed files / ordered migration IDs and checksums: 0019_backup_runs.sql b3d6e59d6fa0
Exact verification commands / environment / actual results:
  node --test tests/backup-restore.test.mjs → 4/4 (V13 no destination → failed row + exit≠0; missing passphrase likewise;
    retention plan; restore into an isolated DB + file dir round-trips and marks restore_tested_at).
Operational state: implemented on branch. NO off-host target configured on the host; timers NOT installed; NOT proven.
Open: Joe picks the target (rclone remote or ssh host) + passphrase → env keys in deploy/README.md; first timed restore drill.
```
