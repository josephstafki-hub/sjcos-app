# A09b — Independent uptime and business-progress monitoring

```text
Date / task / slice: 2026-09-23 / A09b / full slice (independent monitor script + timer, health signals, alert path)
Agent / branch / source version / claimed files: WS-worker (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  scripts/sjcos-monitor.sh, deploy/sjcos-monitor.{service,timer}, lib/worker/health.ts, lib/worker/cron-runs.ts,
  lib/backup/status.ts (alertOnBackupHealth), docs/monitoring.md
Failure reproduced / changed behavior / callers checked:
  Before: only the app could notice the app was down; an idle worker or a missed cron tick raised nothing.
  After: a shell monitor on its own timer checks HTTP, worker heartbeats, cron_runs, backup_runs, source_events backlog,
    unknown intents, decision push failures, stranded agent triggers and overdue obligations, alerting via notifyOwner
    (Telegram) with state-change de-duplication and journal fallback.
Changed files / ordered migration IDs and checksums: none beyond 0019/0020.
Exact verification commands / environment / actual results:
  bash -n scripts/sjcos-monitor.sh → OK; health functions covered by tests/worker-db.test.mjs (workerHealth, cronJobHealth)
  and tests/backup-restore.test.mjs (backupHealth stale/failed). The monitor itself has NOT run against the live host.
Operational state: implemented on branch. NOT installed, NOT proven.
Open: install deploy/sjcos-monitor.timer; verify one forced failure (stop sjcos-worker) produces one alert.
```
