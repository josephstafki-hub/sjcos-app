# A10 — Automatic routine policy and one-tap decision surfaces

```text
Date / task / slice: 2026-09-23 / A10 / full slice (versioned policies, lane pauses, decision cards in app + Telegram + push, routine follow-ups)
Agent / branch / source version / claimed files: WS-approvals (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/commands/policies.ts (foundation), lib/decisions/{cards,routine,telegram,notify}.ts, app/(os)/engine/decisions,
  app/api/telegram (callback), lib/notify (push cards), db/migrations/0004_decision_surfaces.sql, tests/dispatch-db.test.mjs (V14)
Failure reproduced / changed behavior / callers checked:
  Before: routine follow-ups were either fully manual or fire-and-forget; no kill switch; decisions lived only in the app.
  After: policies (key, version, active) decide what is automatic; a missing/conflicting policy HOLDS the action as a decision
    (never silently taken); lane_pauses stop new dispatch per lane; one decision renders identically in the app, Telegram
    inline buttons and push, with the same first-tap-wins resolution and consume-once; lost push → card re-announced by the
    dispatcher pass; routine follow-ups (leads, bidders, invoices) run under their policy with cadence + opt-out checks.
Changed files / ordered migration IDs and checksums: 0001 4c78318f2a17 · 0004 b7f52c1bf352
Exact verification commands / environment / actual results:
  node --test tests/dispatch-db.test.mjs → V14: missing policy → decision retained; repeated cross-channel callback → one
    effect; lost push → re-announced; kill switch → paused lane stops new dispatch. tests/leads-db.test.mjs → routine cadence.
Operational state: implemented on branch. No policies active on production; Telegram callback route NOT deployed. NOT proven.
Open: Joe seeds the first policies on /engine (routine_followup, weekly.client_summary, billing.auto_issue) after deploy.
```
