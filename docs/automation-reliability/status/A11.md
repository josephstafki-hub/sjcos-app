# A11 — Complete lead intake and follow-up

```text
Date / task / slice: 2026-09-23 / A11 / full slice (intake matching, facts collection, follow-up routine, opt-out/decline stops, estimate-input package)
Agent / branch / source version / claimed files: WS-procurement (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/leads/{match,facts,qualification,routine,package,index}.ts, db/migrations/0017_leads.sql, lib/call-notes.ts (ensureCallFollowUps),
  lib/lead-first-response (existing, kept), tests/leads-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: an unknown sender created duplicate leads; asks repeated the same question; a reply/decline/opt-out did not stop the
    next nudge; no traceable package reached estimating.
  After: matchInbound() (email/phone/thread identity, unknown → review item, never a silent new lead); facts recorded with
    sources and asked in the ASKABLE order, each question at most once (ask counted by sent_at); reply processed when facts were
    recorded since the last ask; routine cadence measured against the last send; decline/opt-out/direct owner reply stop the
    routine; buildEstimateInputPackage() hands scope + facts + photos + measurements to A15 with refs; disputes/unusual scope →
    decision, not every email.
  Callers checked: lead-first-response cron (drafts still park for approval; never auto-send unless the policy says so),
    call notes finish() → ensureCallFollowUps (idempotent).
Changed files / ordered migration IDs and checksums: 0017_leads.sql c76a20ab2e05
Exact verification commands / environment / actual results:
  node --test tests/leads-db.test.mjs → 3/3 (V15 reply/decline/opt-out; unknown identity → review; V28 message style rules);
  tests/completion-db.test.mjs → call follow-ups idempotent.
Operational state: implemented on branch. NOT deployed; routine policy NOT active.
Open: shadow mode first (policy lead.routine_followup inactive → drafts only), then Joe arms it.
```
