# A12 — Subcontractor paperwork collection

```text
Date / task / slice: 2026-09-23 / A12 / full slice (requirements, expiry detection, exact-version association, acceptance, restricted storage)
Agent / branch / source version / claimed files: WS-procurement (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/sub-docs/index.ts, db/migrations/0016_sub_documents.sql, sub portal upload route (existing, gated), tests/sub-docs-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: a received file resolved the compliance item; expired/duplicate/wrong-job uploads were not distinguished; any portal
    session could fetch any document by id.
  After: requirement rows per sub/job (COI, W-9, license) with metadata validation (carrier, dates, coverage); receipt ≠
    acceptance (only an accepted document with valid metadata resolves and stops contact); wrong-job / unreadable / expired /
    duplicate produce typed exceptions; COI without a document = missing; FOR UPDATE on the requirement (no double accept);
    documents stored under the restricted prefix; portal access checks sub identity + job (V19).
Changed files / ordered migration IDs and checksums: 0016_sub_documents.sql 344d0fad361a
Exact verification commands / environment / actual results:
  node --test tests/sub-docs-db.test.mjs → 2/2 (V15 expired/wrong document; accepted stops contact; V19 cross-id refused).
Operational state: implemented on branch; NOT deployed; reminders NOT enabled.
Open: professional interpretation (is this coverage sufficient?) escalates with evidence — no auto-accept of COI adequacy.
```
