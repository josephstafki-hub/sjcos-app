# A13 — Procurement, commitments and approved bill payment

```text
Date / task / slice: 2026-09-23 / A13 / full slice (commitments, purchase vs payment decisions, deliveries, bills, cash reservations, buyout, packages, no payment rail)
Agent / branch / source version / claimed files: WS-procurement (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/procurement/{commitments,deliveries,bills,buyout,packages,gate,payment-rail,vendor-rules,types,index}.ts, lib/funding/index.ts,
  db/migrations/0015_procurement_cash.sql, mcp/procurement-tools.mjs (award_bid replaces the legacy direct award),
  tests/procurement-db.test.mjs, tests/funding-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: award_bid flipped a status with no decision; a PO and its later bill shared no authority model; nothing reserved
    cash; no delivery acceptance; vendor bank details could be typed from an email.
  After: commitment lifecycle draft → approved (purchase decision) → committed (cash reserved atomically, construction gate:
    signed agreement + initial payment unless pre-construction service) → acknowledged → delivered (partial/late/wrong/revised)
    → reviewed (owner acceptance sets the payable ceiling) → payable (matched bill) → paid; payment is a SEPARATE decision;
    pay-now bundled only when the preview says so; no rail exists → manual_pending until Joe confirms with evidence; payee
    destinations only from trusted vendor records/validations (untrusted email can never set them); shortfall → explicit
    company-funding decision; buyout deadlines from need dates/lead times/quote validity aligned to expected collections.
  Callers checked: lib/actions/bidding award (owner UI path unchanged), MCP award_bid → decision, funding used by field hooks.
Changed files / ordered migration IDs and checksums: 0015_procurement_cash.sql 306896f53772
Exact verification commands / environment / actual results:
  node --test tests/procurement-db.test.mjs → 6/6 (V22 approved order then bill = distinct authority; changed total/destination
    → fresh decision; repeated pay-now → one; V40 concurrent purchases → atomic limit, no double spend); tests/funding-db.test.mjs
    → 5/5 (pending ACH not spendable; return escalated; company funding explicit).
Operational state: implemented on branch. NOT deployed. purchases/payments lanes exist but nothing dispatches on production.
Open: none in code; vendor payment rail deliberately absent (manual confirmation only).
```
