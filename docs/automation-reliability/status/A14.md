# A14 — QuickBooks Online connection and reconciliation

```text
Date / task / slice: 2026-09-23 / A14 / full slice (adapter fake + Intuit v3, cursor import, proposal-only matching, controlled mirroring, payout reconciliation, settings)
Agent / branch / source version / claimed files: integration (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/accounting/qbo/{types,fake,https,index,sync}.ts, lib/accounting/{server,actions}.ts, app/(os)/settings/accounting,
  components/money/AccountingSettingsButtons.tsx, app/api/cron/qbo-sync, deploy/sjcos-qbo-sync.*, db/migrations/0008_qbo.sql,
  tests/qbo-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: no QBO connection; Houzz/QBO history could only be re-keyed by hand; fees/net deposits had no home.
  After: importPostedEntities() with a cursor (dry-run unless import_read is on) proposes customer/invoice/payment mappings by
    external reference (never by amount) and stages conflict decisions; export of issued invoices / confirmed payments is
    idempotent behind per-direction switches; reconcilePayout(gross, fee, net) = one event; version hashes detect external
    void/edit; connection refresh + summary on /settings/accounting.
Changed files / ordered migration IDs and checksums: 0008_qbo.sql 5141cc166503
Exact verification commands / environment / actual results:
  node --test tests/qbo-db.test.mjs → 1/1 (V16 re-import/export no duplicates; external void → conflict decision; unmatched job
    → proposal only; fee + net reconcile to gross).
Operational state: implemented on branch. NO Intuit app credentials; NOT connected; switches off. NOT proven.
Open: Joe creates the Intuit developer app (INTEGRATIONS.md) → env keys; first import in dry-run.
```
