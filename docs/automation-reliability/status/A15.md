# A15 — Evidence-based estimates and closeout cost learning

```text
Date / task / slice: 2026-09-23 / A15 / full slice (scope register, site findings, design paths, price research, supplier evidence, quotes, assembly, readiness, offer freeze, pricing setup, cost learning)
Agent / branch / source version / claimed files: WS-estimating (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/estimating/{scope,design,pricing,fetcher,fetcher-live,assembly,readiness,setup,learning,rules,types,index,actions}.ts,
  db/migrations/0010_estimating_records.sql + 0011_estimating_seed_supplier_knowledge.sql, mcp/estimating-tools.mjs,
  app/api/internal/estimating (research bridge), app/(os)/projects/[slug]/scope, app/(os)/settings/pricing + components/settings/PricingSetup.tsx,
  tests/estimating-db.test.mjs, tests/estimating-rules.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: estimate lines were typed; no scope allocation record; prices had no source/date; competing quotes could both land;
    a sent offer could be repriced; no pricing setup versioning; no closeout learning.
  After: scope register from lead facts (unverified until site findings), Joe's allocations + dedicated prices with basis
    (ambiguous basis → question), source-linked site findings; design path per scope; researchPrice() records dated, sourced
    observations (never substitutes, never supplies a physical quantity); supplier knowledge in three separated evidence levels;
    quotes → incorporate (noncompetitive replaces provisional cost; competing → held + one supplier_choice decision); sub bids
    → scope lines with exclusions as gaps; estimateReadiness() scores evidence (hard/soft gaps, provisional, allowances, margin);
    freezeOfferedPrices() at send → later supplier costs move margin only; allowance overage → CO payload + decision; pricing
    setup versions (activation = markup decision); closeout actuals → cost learning with outlier/small-sample guards, markup
    changes always a decision.
  Callers checked: workflow engine (prepareFromSignedPrecon → register + formal estimate + scope lines), closeout server →
    learning.ingestCloseoutActuals, MCP tools (23), scope page, pricing settings page.
Changed files / ordered migration IDs and checksums: 0010 61aa5f89a9d7 · 0011 c0256e016c99
Exact verification commands / environment / actual results:
  node --test tests/estimating-db.test.mjs → 6/6; tests/estimating-rules.test.mjs → 15/15 (V17 missing scope, stale/zero
    unknown price, wrong units, changed design; V32/V33/V34/V36/V37 rules; V25 repeated closeout/outlier/changed markup).
Operational state: implemented on branch; NOT deployed; learning.cost_update policy NOT active; no active pricing setup.
Open: Joe proposes + activates pricing setup v1 on /settings/pricing after deploy.
```
