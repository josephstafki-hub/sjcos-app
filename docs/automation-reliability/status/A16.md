# A16 — Sub portal field evidence, scheduling and weekly client summaries

```text
Date / task / slice: 2026-09-23 / A16 / full slice (field reports, completion claims, snags, schedule plans/gates/adjustments, buyout dates, weekly summaries)
Agent / branch / source version / claimed files: WS-field (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/field/{reports,incidents,schedule-plans,weekly-summary,apply-decisions,dates,hooks,server,index}.ts,
  db/migrations/0012_field_schedule.sql, app/api/internal/field (bridge), mcp/field-tools.mjs, app/api/cron/weekly-summary,
  deploy/sjcos-weekly-summary.*, tests/field-db.test.mjs, tests/field-fixtures.mjs
Failure reproduced / changed behavior / callers checked:
  Before: sub progress lived in free text; a completion claim could be billed; snags had no decision; schedule dates were typed;
    weekly updates were manual and could include private photos.
  After: field_reports with deduped photos (sha256) + visibility; completion claim → checks existing photos, asks only the
    missing views once, stages Joe's milestone confirmation (billing hook fires only on his confirmation); snags → immediate
    owner alert with facts/impact/recommendation + continue/pause decision (agent never decides); schedule plans tentative →
    approval → confirmed only with signed agreement + initial payment; internal adjustments auto only when no promise changes
    and cost does not rise; buyout obligations from lead times; weekly client summary assembled from verified client-safe
    evidence, snags held to Joe first, sha-deduped, one per week under its policy.
  Callers checked: billing (issueProgressOnOwnerConfirmation), funding, workflow engine (contract → W09), MCP (13 tools).
Changed files / ordered migration IDs and checksums: 0012_field_schedule.sql 7e142c921644
Exact verification commands / environment / actual results:
  node --test tests/field-db.test.mjs → 6/6 (V23 duplicate upload/conflicting progress/urgent delay/private file; V39 gates;
    V41 sufficient evidence, missing niche photo, repeated timer; V42 harmless move vs downstream sub; V18 Central midnight/DST).
Operational state: implemented on branch; NOT deployed; weekly.client_summary policy NOT active.
Open: sub portal UI for photo upload uses the existing sub-log form (mapped by sub_log_id); native camera flow is mobile work.
```
