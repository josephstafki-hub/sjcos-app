# A17 — Closeout, warranty, signed documents and approved marketing

```text
Date / task / slice: 2026-09-23 / A17 / full slice (closeout checklists, walkthrough gate, sign-off → final invoice, post-project, immutable signed revisions, marketing approvals)
Agent / branch / source version / claimed files: WS-field (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/closeout/{checklist,postproject,revisions,marketing,hooks,server,index}.ts, db/migrations/0013_closeout.sql,
  app/api/cron/post-project, deploy/sjcos-post-project.*, mcp/field-tools.mjs (closeout_step), tests/closeout-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  Before: no punch/walkthrough sequence; a second sign-off could bill twice; edited signed PDFs; marketing reuse assumed.
  After: internal inspection → corrections (Joe confirms) → client walkthrough (refused before confirmation) → client punch →
    written sign-off (immutable document_revisions pin) → final invoice once (verified remainder) → post-project ticks
    (review request day 0, warranty sources on record only, check-ins; reply with issue → warranty claim + escalation); actuals
    ingested once per revision (canonicalJson compare) → cost learning; marketing publication needs media permission +
    audience approval, withdrawn permission blocks a queued post.
Changed files / ordered migration IDs and checksums: 0013_closeout.sql 0a6d99700a45
Exact verification commands / environment / actual results:
  node --test tests/closeout-db.test.mjs → 3/3 (V44 open punch item; corrected-not-confirmed; duplicate sign-off; V45
    warranty sources, review request, late actual cost revised once; V29 edited signed artifact refused, withdrawn media).
Operational state: implemented on branch; NOT deployed; post-project timer NOT installed.
```
