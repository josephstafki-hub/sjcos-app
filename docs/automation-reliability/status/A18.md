# A18 — Baseline, overhead, learning governance and truthful procedures

```text
Date / task / slice: 2026-09-23 / A18 / full slice (measurement cases, owner touches, overhead, learning governance, procedure checks, capability status)
Agent / branch / source version / claimed files: WS-measure (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/measure/{cases,baseline,learning,procedures,capabilities,math,server,actions}.ts, lib/overhead/{overhead,actions}.ts,
  db/migrations/0018_measurement.sql, app/(os)/engine/{measure,capabilities}, app/(os)/settings/overhead, mcp/measure-tools.mjs,
  scripts/list-mcp-tools.mjs, scripts/record-capability-evidence.mjs, docs/capabilities.md, tests/measure-db.test.mjs, tests/measure-rules.test.mjs
Failure reproduced / changed behavior / callers checked:
  After: measurement cases with eligible/verified/corrected/missed/unknown outcomes (failed cases stay in the denominator;
    manual review counts as owner time); owner_touches from decisions/corrections; overhead: fixed subscriptions (Anthropic
    $200, OpenAI $10 seeded as owner-reported) separate from metered charges, reconciled by external ref only (no double count);
    thresholds notify only; procedure snapshots + checks (missing tools via the MCP tool list, retired fields, contradictions);
    memory classes (one-job preference / factual correction / proposed rule) never auto-promoted; capability_status with four
    independent evidence-backed states.
Changed files / ordered migration IDs and checksums: 0018_measurement.sql e8d8917f7d18
Exact verification commands / environment / actual results:
  node --test tests/measure-db.test.mjs → 6/6; tests/measure-rules.test.mjs → 6/6 (V20 failed case in denominator; manual
    correction = owner time; duplicate subscription import refused; unknown estimates labelled).
Operational state: implemented on branch; NOT deployed; capability rows all false until scripts/record-capability-evidence.mjs
  is run by the owner after deploy (implemented only) and Joe records deployed/enabled/proven himself.
```
