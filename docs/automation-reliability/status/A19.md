# A19 — Owner site and office time capture

```text
Date / task / slice: 2026-09-23 / A19 / full slice (intervals, location prompts, designer activity, manual timers, review UI, mobile contract, costing)
Agent / branch / source version / claimed files: WS-field (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/owner-time/{intervals,location,designer,costing,server,actions,index}.ts, db/migrations/0014_owner_time.sql,
  app/(os)/time + components/time/TimeReview.tsx, app/api/time/events, app/api/mobile/time/{events,timer,review},
  components/floor/ActivityEmitter.tsx (+ mount in components/floor/Designer.tsx), docs/mobile-time-contract.md, tests/owner-time-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  After: one running manual/site timer per user; every clock action replay-safe on client_event_id; location → prompt after
    dwell (several jobs → choices, never auto-pick), drive-by discarded, boundary flapping → one prompt, exit suggests an end
    (never clocks out); designer focus/heartbeat/idle/blur/end → bounded 'design' intervals with idle gaps as review rows,
    duplicate tabs merged, overlap with a site timer flagged; corrections append history; hours counted as a UNION; valued only
    by a dated approved rate ("cost not configured" otherwise); verifiedHoursForCloseout → A15 with provenance; no payroll,
    client charge or QBO expense.
  Callers checked: closeout actuals (verified hours), Designer (owner + editable only), mobile app (contract doc; not yet built).
Changed files / ordered migration IDs and checksums: 0014_owner_time.sql d4a4690ab2e1
Exact verification commands / environment / actual results:
  node --test tests/owner-time-db.test.mjs → 2/2 (V24 drive-by, denied permission, offline replay, idle tab, two devices,
    site/office overlap; V26 designer session → correct job/revision).
Operational state: implemented on branch; NOT deployed; phone side NOT built (sjcos-mobile) and background detection NOT
  device-proven; feature.owner_time stays implemented only.
Open: mobile app endpoints per docs/mobile-time-contract.md; physical-device acceptance list in that doc.
```
