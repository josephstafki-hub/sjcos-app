# A20 — Square card and ACH customer payments

```text
Date / task / slice: 2026-09-23 / A20 / full slice (invoice-bound checkout, fake + https adapters, webhook verification, reconciliation, refunds as decisions, settings)
Agent / branch / source version / claimed files: WS-money (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/payments/{service,server,actions}.ts, lib/payments/square/*, app/(os)/settings/payments, app/(os)/client-portal/pay,
  app/api/square webhook, app/api/cron/payments-reconcile, deploy/sjcos-payments-reconcile.*, db/migrations/0007_payments.sql,
  tests/payments-db.test.mjs, tests/square-signature.test.mjs
Failure reproduced / changed behavior / callers checked:
  After: server computes the allowed outstanding amount per invoice revision; every attempt maps to that revision; browser paid
    flag/amount never trusted; card + US bank transfer; pending/completed/failed/returned/refunded/disputed states; webhook
    signature verified, missed events reconciled by the cron; repeat taps/webhooks/timeouts → one charge; late failure/return
    restores the balance without a new invoice; refunds → one-tap decision; unconfigured vs ready clearly distinguished;
    gross/fee/net payouts flow to A14.
Changed files / ordered migration IDs and checksums: 0007_payments.sql 978efdf401fd
Exact verification commands / environment / actual results:
  node --test tests/payments-db.test.mjs → 3/3 (V21 repeat checkout, stale amount, missed/repeated callback, pending ACH not
    paid, failed/returned/refunded); tests/square-signature.test.mjs → 2/2.
Operational state: implemented on branch. NO Square account (merchant approval required); NOT deployed; NOT proven.
Open: Joe opens the Square account → sandbox keys → /settings/payments shows "ready"; live use only after merchant approval.
```
