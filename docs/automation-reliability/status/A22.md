# A22 — Delegated approvals and employee accounts

```text
Date / task / slice:
  2026-09-23 / A22 / full slice (authority catalog + grants + revocations + audit + Team UI + finance fences)
Agent / branch / source version / claimed files:
  WS-access (Claude) / t3code/build-sjc-os-plan (worktree t3code-b015e50c) / base edac657
  Owned + edited: lib/permissions.ts, lib/dal.ts, lib/api-auth.ts, lib/actions/users.ts,
    lib/authority/* (catalog.ts, grants.ts, commands.ts, mcp-gate.ts, usage.ts, worker-identity.ts,
    run-profile.mjs, index.ts), components/settings/TeamAccess.tsx, components/settings/Authority.tsx,
    app/(os)/settings/team/[userId]/page.tsx, docs/users-and-access.md, db/migrations/0009_access.sql,
    tests/access-db.test.mjs
  Unowned pages edited surgically for the finance fences (flagged for the integration owner, see below):
    app/(os)/leads/[slug]/page.tsx, app/(os)/projects/[slug]/page.tsx, app/(os)/subs/page.tsx,
    app/(os)/subs/[slug]/page.tsx
Failure reproduced / changed behavior / callers checked:
  Before: the only authority concept was role=owner; authority_grants existed (0001) with no writer, no UI,
    no audit; a staff member holding the `estimates` area saw the lead rough estimate and sub rates; a
    disabled/changed account kept working until the JWT expired (7 days); permission edits could be made by
    any code path holding a pool.
  After:
    • lib/authority/catalog.ts — 12 action types mirroring DECISIONS.md kinds; `grant` is owner-only, never a row.
    • lib/authority/grants.ts — grantAuthority / revokeAuthority (assertAuthorityAdmin: principal.kind==='user'
      AND role==='owner', never an agent even for the owner, never self, never '*'/'grant'), listUserAuthority,
      effectiveAuthority (same semantics as decisions.ts authorityFor for non-decision callers, account row
      re-read every call), revokeSessions / sessionRevokedSince, auditPermissionChange.
    • lib/authority/commands.ts — the above as keyed commands (lib/commands/db command()) with audit rows.
    • lib/dal.ts getCurrentUser + lib/api-auth.ts getUserFromRequest — a JWT minted before the user's newest
      session_revocations.revoked_before is treated as no session (sessionRevoked(); authAt claim, else iat;
      no mint time = fail closed).
    • lib/actions/users.ts — grantAuthorityAction / revokeAuthorityAction / revokeUserSessionsAction (owner-only);
      updateUserAccess now audits and signs the person out when an area is REMOVED; setUserActive audits and
      revokes sessions on disable.
    • UI: /settings/team/<userId> (owner-only via OWNER_ONLY_PATHS + requireRole): "Areas · what they can open"
      and "Authority · what they may approve" as two sections + permission history; Access modal links to it.
      New accounts default to no authority (nothing is inserted on create).
    • Finance fences: lead rough estimate card + "Rough estimate" tab → can(viewer,'estimates'); project
      Documents › lead paperwork → showEstimates; subs directory rate chip + sub detail Rate card / Pricing tab
      → can(viewer,'money').
    • lib/permissions.ts `ai` description rewritten (business profile only); OWNER_ONLY_PATHS unchanged.
  Callers checked: decisions.ts resolveDecision (app/telegram/push/mcp all route through authorityFor — a
    revoked grant refuses a stale button); lib/commands/db agentPrincipal (unknown/inactive id → unattended);
    /engine/permissions page (requireRole owner, unchanged); WS-approvals' /engine/decisions uses authorityFor
    (not edited by me).
Changed files / ordered migration IDs and checksums:
  db/migrations/0009_access.sql — sha256 07ffbbe3555f9b69… (full: `sha256sum db/migrations/0009_access.sql`)
    users.ai_scope jsonb, users.last_permission_change_at; session_revocations; permission_audit;
    authority_grants CHECKs (action_type ≠ '*', shape; max_amount_cents > 0); dev_agent_runs.profile +
    principal_user_id (+ CHECK operator|business); agent_usage.
Exact verification commands / environment / actual results:
  export SJC_TEST_CLUSTER_TAG=access
  node scripts/test-db.mjs reset                      → OK (schema + 0000–0022 load)
  node --test tests/access-db.test.mjs                → 6/6 pass (own cluster tag "access-access-suite")
  node --test tests/agent-profile.test.mjs            → 8/8 pass
  node --test --test-concurrency=1 "tests/**/*.test.mjs" → 303 tests, 301 pass, 0 fail, 2 skipped
  npm test (parallel)                                 → my suites pass; 10 failures in OTHER workstreams' DB
    suites (billing, commands-core, measure) caused by every DB suite sharing one cluster/database and
    truncating each other mid-test. Not a code failure: the same files pass serially. Fix suggestion below.
  npx tsc --noEmit                                    → no errors in my files. Pre-existing errors in other
    workstreams' files: lib/decisions/cards.ts, lib/field/reports.ts, app/api/cron/_lib/guard.ts,
    lib/closeout/checklist.ts, lib/estimating/assembly.ts, lib/procurement/packages.ts.
  npx eslint <all my files>                           → clean.
Sanitized evidence references / remaining untested conditions:
  tests/access-db.test.mjs: V27 (staff with estimates area, no authority: UI path + agent path refused, decision
    stays pending), bounds (amount / project / kind) on decision AND pure callers, revoke → authorityFor refuses
    the stale button + sessionRevokedSince true for a token minted a minute earlier, admin refusals (staff, agent-
    for-owner, service, self, 'grant', '*', negative cap; the table CHECK refuses '*' even if the module were
    bypassed), V12 malicious email = data.
  Untested live: the /settings/team page render and the Team modal link (no dev server started per the rules);
    a real browser session being refused after revoke (unit-level covered via sessionRevokedSince; the dal/api-auth
    branch is 4 lines over that function).
Setup / activation / deployed version / policy scope (or not performed):
  Not deployed. Activation = `node db/migrate.mjs` (0009) on the live DB, build + restart sjcos.service. No
  policy seeds. No behaviour changes for the owner's own account.
Rollback / compatibility / unknown effects requiring reconciliation:
  Migration is additive (new tables/columns/CHECKs). Rollback = drop session_revocations / permission_audit /
  agent_usage, drop the two authority_grants CHECKs and the dev_agent_runs profile columns; code rollback is
  a plain revert. Compatibility: every new column has a default; old app code ignores them. The authority_grants
  CHECK would refuse a pre-existing '*' row — none can exist (no writer existed before).
Remaining acceptance criteria / dependency or setup blocker:
  • Session revocation vs proxy renewal: proxy.ts re-signs a session after 24 h with a fresh iat and copies only
    userId/role/perms. Integration owner: (1) lib/session.ts createSession → add `authAt: Math.floor(Date.now()/1000)`
    to the payload; (2) proxy.ts renewal() → carry `authAt: payload.authAt` forward. dal/api-auth already prefer
    authAt over iat. Until then the check still bites on the very next request (dal reads the request cookie),
    and /logout clears the renewed one.
  • Staff cannot poll their own runs: lib/actions/dev-agents.ts pollAgentRun / stopAgentRun / answerInteraction
    are requireRole("owner"), while sendMessageAction is requireAccess("ai"). Suggest requireAccess("ai") plus
    `run.principalUserId === user.id || user.role === 'owner'` (DevAgentRun now exposes principalUserId/profile).
  • Selection budgets still visible to staff without `money`: components/projects/SelectionsBoard.tsx renders
    view.overallBudget and per-section budget/remaining. Exact edit: add `showBudget: boolean` prop; project page
    passes `showBudget={can(viewer,"money")}`; wrap the budget bar block (lines ~230–312), the "Set budget"
    button and the per-section `g.budget > 0` lines (~568–600) in `showBudget &&`. Vendor pricing on vendor
    pages and doc drafts containing prices remain (listed in docs/users-and-access.md "Known gaps").
  • WS-approvals' /engine/decisions: scoped approvers rely on authorityFor (unchanged); the "May approve"
    labels from lib/authority/catalog.ts can be used for the decision cards' authority hint.
  • AGENTS.md "Claude in the app": add one line — a STAFF user's Ask window runs the business profile only.
  • Test harness: parallel DB suites collide on one cluster. Either `"test": "node --test --test-concurrency=1 …"`
    in package.json, or each DB suite sets `process.env.SJC_TEST_CLUSTER_TAG += "-<suite>"` before a dynamic
    import of the harness (pattern in tests/access-db.test.mjs).
Next precise ready slice:
  Decision-card authority hint + "approvable by" list on /engine/decisions using listUserAuthority; staff
  self-view of their own authority (read-only card on /today) once pollAgentRun is opened to staff.
```
