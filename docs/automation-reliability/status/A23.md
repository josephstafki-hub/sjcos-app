# A23 — Confirmed lead-to-closeout workflow and proactive estimate assembly

```text
Date / task / slice: 2026-09-23 / A23 / full slice (project workflow join, W02 preparation on signature, derived gates/blockers, MCP + cross-workstream wiring)
Agent / branch / source version / claimed files: integration (Claude) / t3code/build-sjc-os-plan @ ab35754 /
  lib/workflow/{engine,server}.ts, db/migrations/0023_workflow.sql, mcp/workflow-tools.mjs, lib/actions/esign.ts (applySignature hook),
  mcp/sjcos-mcp.mjs registrations, mcp/{estimating,procurement,billing,field,context}-tools.mjs, mcp/tool-shared.mjs,
  app/api/internal/{field,estimating}, tests/workflow-db.test.mjs
Failure reproduced / changed behavior / callers checked:
  After: one project history (project_workflows + events); a verified SIGNED pre-con starts W02 (project from lead if needed,
    scope register, site-visit plan, design path per scope, formal estimate structure with explicit gaps) without payment or a
    site visit, idempotent on re-signature; formal estimate signature → W08, contract → W09, completion → W12; every signature
    enqueues an agent trigger; projectWorkflowView() derives gates/blockers/pending decisions/next actions; 228 MCP tools now
    cover every feature contract (estimating, procurement/cash, billing, field/schedule/closeout, context); cross-workstream
    wiring: grant gate on sends, evidence-backed done from MCP, field hooks → funding/billing, closeout → learning, decisions →
    agent triggers, Hermes turns carry the operating block.
Changed files / ordered migration IDs and checksums: 0023_workflow.sql c9a7b15fba88
Exact verification commands / environment / actual results:
  node --test tests/workflow-db.test.mjs → 3/3 (V31 signed pre-con with unpaid invoice/no visit → one register, plan, design
    work; duplicate + revoked/invalid signature cannot start; contract → W09; completion → W12); MCP smoke boot against the
    harness: 228 tools, 14 new tools answered (mcp smoke 2026-09-23).
Operational state: implemented on branch; NOT deployed; NOT proven on a real signature.
```
